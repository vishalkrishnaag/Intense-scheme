;; src/test/java/test/test.clj
;; (import "src.test.java.test.test" as test)
;; (package "test")
;; (import "org.jetbrains.annotations.NotNull" as  not-null)

;; (enum colors  {:orange 21 :apple 21 :mango 24 :default 0})
;; (@not-null 
;;  def -testing:None ()
;;    (println "raju bhai hero daa"))

;; (def sri:Int
;;    [using x:Int  y:Int]
;;    (var vis:Int (add? x y))
;;    (set! vis (add? 23 31))
;;    (println (add? "output " vis))
;;    (var intensive:String "hello world")
;;    (val dongs:Int 32)
;;   (return x))

(class Vikram is
(var my-test-data:String is "hello world")
(def activity:None [using Input:Int]
 (var my-test-data:String is this.my-test-data)
))

(class test is
  (def racoon:None?
  (using var1:Any?)
  (println (add 21 32))
  (println (Vikram.activity 323))
  (println var1)
  (var var2:Vikram? is null)
  (var my-list:Any is [2123 232 321 321 321])
  (return null))

  ;; (def main:None [] is null)


(def main:None []
 (var test1 test)
 (test1.racoon "raju bhaji hero daa \t hero ")))