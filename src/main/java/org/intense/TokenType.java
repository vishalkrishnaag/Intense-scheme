package org.intense;

public enum TokenType {
    LPAREN, RPAREN,
    LLIST, RLIST,
    LBRACE, RBRACE,
    SYMBOL, NUMBER,
    STRING, BOOLEAN,
    QUOTE, QUASIQUOTE,
    UNQUOTE, UNQUOTE_SPLICING,
    IMPORT,
    COLON, EOF,
    IF, DEF, THIS,
    INT,
    FLOAT,
    LONG,
    DOUBLE,
    OBJECT,
    ARRAY_INT,
    ARRAY_DOUBLE,
    ARRAY_OBJECT,
    SET,
    PACKAGE,
    WHILE,
    FOR,
    MAP_KEY,
    VAR,
    VAL,
    SELF,
    NULL,
    BYTE,
    SHORT,
    UBYTE,
    ULONG,
    NULLABLE,
    DOT,
    STRING_KEYWORD,
    INT_KEYWORD,
    DOUBLE_KEYWORD,
    BOOLEAN_KEYWORD,
    FLOAT_KEYWORD,
    BYTE_KEYWORD,
    SHORT_KEYWORD,
    LONG_KEYWORD,
    UBYTE_KEYWORD,
    ULONG_KEYWORD,
    RETURN,
    USING, CHAIN, AS, ENUM, CLASS, FOREACH, GET, ANY_KEYWORD, LESS_THAN, GREATER_THAN, TRY, CATCH, IS, EXTENDS, INTERFACE, LAMBDA,
}
